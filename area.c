#include<stdio.h>
void main()
{
	int side,area,perimeter;
	side=2;
	area=side*side;
	perimeter=4*side;
  printf("area of square with side %d is %d\n",side,area);
  printf("perimeter if the square with side %d is %d",side,perimeter);
}
